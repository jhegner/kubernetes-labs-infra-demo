apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-micronout
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/jhegner/kubernetes-labs-infra-demo.git
    targetRevision: k3d
    path: istio/infra
  destination:
    server: https://kubernetes.default.svc
    namespace: labs
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
