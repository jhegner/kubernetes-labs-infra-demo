name: Demo Terraform Vultr Connect

on:
  push:
    branches:
      - demo_vultr_terraform

jobs:
  terraform-execute:
    runs-on: ubuntu-latest
    env:
      TF_LOG: TRACE
      VULTR_API_KEY: ${{ secrets.K8S_LABS_VULTR_API_KEY }}
      FAKE_SECRET: ${{ secrets.FAKE_SECRET }}

    steps:
      # - name: Checkout repository
      #   uses: actions/checkout@v4

      # - name: Set up Terraform
      #   uses: hashicorp/setup-terraform@v3

      # - run: echo "PRINT FAKE SECRET $FAKE_SECRET"
      - run: echo "PRINT FAKE ENV VAL $MY_VARIABLE"

      - name: Test pass
        run: |
          # Test if the variable is set (DO NOT print its value)
          if [ -z "$FAKE_SECRET" ]; then
            echo "Error: FAKE_SECRET is not set!"
            exit 1
          fi  
          curl -H "Authorization: Bearer $FAKE_SECRET" https://api.example.com/data    

      # - name: Terraform Init
      #   run: terraform init        
      # - run: echo "🎉 Terraform initialized"

      # - name: Terraform Validate
      #   run: terraform validate

      # - name: Terraform Plan
      #   run: terraform plan -out=tfplan          

      # - name: Terraform Apply
      #   run: terraform apply -auto-approve tfplan        